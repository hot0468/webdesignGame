---
name: project-context
description: 웹디(웹디자이너 회사 운영 게임) 프로젝트의 압축 컨텍스트 — 확정된 설계 결정, 기술 스택, 디자인 토큰, 코딩 컨벤션, 상세 참조 파일 목차. 게임 기능의 구현/수정/설계/디버깅 등 코드를 만지는 모든 작업 전에 반드시 이 스킬을 로드할 것. 코드베이스 탐색 대신 이 문서를 읽는 것이 토큰 절약의 핵심이다. 설계 결정이 바뀌거나 새 파일/패턴이 생기면 이 문서 또는 해당 참조 파일을 즉시 갱신할 것.
---

# 프로젝트 컨텍스트 (단일 진실 공급원)

이 문서가 코드베이스 탐색을 대체한다. **여기 없는 정보는 아래 참조 파일에서 찾고, 거기에도 없을 때만 파일을 직접 읽어라.**

**갱신 의무:** 설계 결정 변경·새 파일·새 패턴 발생 시 같은 커밋에서 이 문서 또는 해당 참조 파일을 갱신한다.
**작성 규칙:** **규칙만 적는다.** 어떻게 알아냈는지·무엇을 실측했는지·어떤 대안을 기각했는지는 적지 않는다(단, "되살리지 말 것"은 규칙이므로 남긴다). 사연의 정본은 **그 결정이 사는 소스 파일의 주석 하나**다 — CLAUDE.md·HISTORY.md·커밋 메시지에 다시 풀어 쓰지 않는다.
**상한: 이 파일 12KB.** ⚠️ 상한은 줄 수가 아니라 **바이트**다 — 줄 수 상한은 줄이 가로로 자라며 무력화됐다(겪었다). 넘기면 새 내용을 더하지 말고 **먼저 같은 분량을 참조 파일로 내리거나 지운다.** 이 파일은 모든 작업이 통째로 읽으므로 크기가 곧 비용이다. **새 시스템의 규칙 전문은 처음부터 `references/systems.md`에 적고 여기엔 색인 한 줄만 더한다.**

## 원본 기획
[docs/SPEC.md](../../../docs/SPEC.md) — 설계자 브리프 원문. **요구사항의 정본이다.**
스펙에 없는 것을 구현자가 정했으면 그 결정은 이 문서에 적는다(스펙 원문은 고치지 않는다).

## 현재 상태
**OS 셸 + 게임 시스템 전부가 돈다**(업무·공정·직원·수주센터·정산·세이브·게임오버). 규칙은 `references/systems.md`가, 셸·파일 맵은 `references/shell.md`가 진다.
코어 설계 정본: [docs/superpowers/specs/2026-08-10-webdi-core-design.md](../../../docs/superpowers/specs/2026-08-10-webdi-core-design.md)

## 게임 컨셉
- 웹브라우저용 **회사 운영 게임**. UI 전체가 가짜 윈도우 OS다 — 플레이어는 웹디자이너 1인 회사 대표
- 바탕화면의 프로그램(피그마·포토샵·VS코드·카톡·네이트온·아웃룩·일정·브라우저·작업물 폴더)이 곧 게임 메뉴다
- 톤: **귀엽고 트렌디**. ⚠️ windowsGame의 뉴트럴 OS 크롬과 다른 방향이다 — 코드를 베껴 오더라도 팔레트는 베끼지 않는다

## 확정된 설계 결정 — 색인
**규칙 전문은 [references/systems.md](references/systems.md)다.** 아래는 한 줄 색인이고, **그 축을 만지는 작업은 systems.md의 해당 절을 반드시 먼저 읽는다** — 한 줄만 보고 구현하면 상세 불변식(씨앗 규칙·clamp 자리·판정 위치)을 깬다. **여기에도 systems.md에도 없으면 아직 안 정해진 것이다.**

| 축 | 한 줄 | systems.md 절 |
|---|---|---|
| 시간 | 주 단위 턴, 진행은 `advanceWeek` 하나(사람의 선택 — 자동 넘김 금지). 주말은 확률 돌발 의뢰. 한 달=4주 고정, 공휴일 직전 주는 팝업이 몰리는 피크타임 | §시간·주차 |
| 자원 | 행동력(=시간, 별도 시간 자원 금지)·정신력(0은 게임오버 아님, 나쁜 일에도 깎인다)·소지금(손으로 나가는 곳은 쇼핑몰 하나). 행동력 상한 정본은 `apMaxOf` 한 함수 | §자원 |
| 회사 | 평판(0~100)·레벨(누적 매출→행동력 상한·**사이트 해금**)·등급(평판→채용 상한). 셋 다 파생 — 저장하는 건 평판·`revenue`뿐 | §회사 축 |
| 게임오버 | 파산(급여 3달 연속 미납)·폐업(평판 위기 4주, 명시적 카운터). **엔딩·도감 없음 — 만들지 말 것** | §게임오버 |
| 스탯 | ⚠️ **등급은 스탯**(디자인/퍼블리싱/CS), **비용 감면은 숙련도**(피그마/포토샵/코딩) — 뒤바꾸는 것이 이 게임의 단골 사고. 비용 정본은 `apCost` 한 함수 | §스탯·숙련도 |
| 수주 | ①메일·고객게시판·톡톡 직접 의뢰 ②수주센터 입찰(소기업 이상, 씨앗=공고 id). 모든 의뢰에 기한 — 초과는 계약 파기(위약금 없음) | §수주 |
| 공정 | 업무 = 공정의 줄(순서 고정). 퀄리티 3택이 밴드, 스탯이 칸(`gradeOf` — 무작위 없음). **공정 완료≠업무 완료 — 완료는 회신이고 `completeJob`을 부르는 것도 회신** | §공정·납품 |
| 미팅 | 사이트 업무만. 키워드 정답은 저장 안 함(씨앗=업무 id), 기획력이 알아내는 개수를 정함(전부는 못 앎) | §미팅·키워드 |
| 팝업 | 제작→관리자 페이지 등록→주차 판정→클레임. 등록 기간은 "주장"이고 의뢰와 어긋나면 클레임. 불변식 `dueWeeks > toWeeks` | §팝업 |
| 메일 | 게임 중 생긴 글은 스토어 `mails`·`claims` — 뱃지 세는 곳은 `unreadCount`에 반드시 넘긴다. 메신저 상대는 직원, 톡톡 상대는 클라이언트 | §메일·메신저 |
| 직원 | 지시(행동력 1 고정, N주 뒤, 점유 정본은 `orders`)·교육(레벨업 유일 경로, 월급 영구 상승)·요청(거절·무시에 `grudge` 대가) | §직원 |
| 돈 | 월말 "유지보수보고서" 정산 — 급여는 직원 목록에서, 유지보수 계약이 고정 수입, **월 투자**가 후반 지출. CS는 사과와 수정 확률에 산다 | §돈·정산 |
| 후속 | 수정 요청(`step` 되돌림 — 그 회신은 무효, 대금·평판 안 움직임)·성격 4종(업체 이름 파생)·버그 신고(미래 주차 메일) | §후속 요청 |
| 특수 이벤트 | **좋은 일 둘(소개·수상)·나쁜 일 하나(저작권)**. 소개가 거래처를 늘리고 그 목록(`clients`)이 이벤트가 업체를 고르는 풀이다 | §특수 이벤트 |
| 브라우저 | 즐겨찾기는 스토어 `bookmarks`. 바로가기는 회사레벨로 잠기고 주소 직접 입력도 막는다 | §브라우저·사이트 |
| 세이브 | ⚠️ **새 상태 축 = `partialize`/`saveFields`에도 추가**(빠지면 그 축만 조용히 안 남는다). 슬롯 3칸은 자동저장과 다른 키 | §세이브 |
| 업체 목록 | **정본은 `clientsOf(jobs, clients)` 하나다** — 거래처(소개로 열림) + 일을 준 곳(수주센터 낙찰 포함, 접속 정보는 이름에서 파생). 사내시스템·에디터·FTP가 같은 함수를 본다 | §특수 이벤트·§공정·납품 |
| 무작위 | 전부 씨앗 받는 순수 함수(`systems/seed.ts`의 `roller`가 유일한 난수원). `Math.random` 금지 | 각 절의 씨앗 규칙 |

## 미결 (범위 밖 — 필요해질 때 정한다)
- 브라우저 탭

## 디자인 토큰
⚠️ **CSS는 정의되지 않은 var를 만나면 그 선언 전체를 무효로 만든다** — 없는 토큰을 쓰면 빌드도 테스트도 통과하고 **화면에서만** 조용히 깨진다.
- 단일 출처는 `src/index.css`의 `:root`. **컴포넌트 CSS에 생 hex나 임의 px을 새로 적지 않는다**
- **시각 언어를 섞지 않는 것**이 "게임이 OS 위에 얹혀 있다"는 인상의 근거다. **사이트와 프로그램(피그마·포토샵·VS코드 등)은 예외**로 자기 팔레트를 자기 CSS 파일 안에 가둔다
- ⚠️ 반투명 표면 위 글자색은 토큰이 아니라 **합성 결과**가 정한다. 눈이나 계산이 아니라 픽셀로 판정한다

## 아이콘
- ⚠️ **UI에 이모지를 쓰지 않는다.** Iconify 오프라인 번들만 쓴다(**CDN 금지, 외부 이미지 금지**)
- 상세는 `references/icons.md`

## 코딩 컨벤션
1. **게임 수치는 전부 `src/data/`에 있다.** 컴포넌트에 숫자를 적으면 밸런스 테스트가 못 보는 두 번째 출처가 생긴다
2. **`src/systems/`는 순수 함수다.** React import 금지, 상태 mutation 금지, `Math.random` 금지(무작위가 필요하면 **시드를 받는다** — 이 게임은 업무 발생·주말 돌발 이벤트·지원자 생성이 전부 무작위라 이 규칙이 특히 중요하다)
3. **동작하지 않는 컨트롤을 그리지 마라.** 눌러도 아무 일 없는 버튼·갈 데 없는 링크는 만들지 않는다
4. **관계는 한 방향으로만 적는다.** 반대쪽은 파생 함수로 찾는다
5. 창 UI는 반드시 공용 `Window` 컴포넌트를 쓴다

## 참조 파일 — 필요할 때만 읽는다
`.claude/skills/project-context/references/` 아래에 있다. **작업과 무관한 파일은 열지 않는다.**

| 파일 | 언제 읽나 |
|---|---|
| `systems.md` | 게임 규칙(시간·자원·수주·공정·팝업·미팅·직원·돈·후속·세이브)을 만지는 작업 — **만지는 축의 절만** 읽는다 |
| `icons.md` | 아이콘 이름을 추가·변경하거나 세트를 고를 때 |
| `shell.md` | 화면을 고치거나 새 프로그램 창을 붙일 때(시각 언어 출처·대비 규칙·셸 구조·파일 맵·실측 스크립트) |

⚠️ `sites.md`(브라우저/사이트) · `systems.md`(업무·직원·정산)는 그 코드가 생길 때 만들어 이 표에 추가한다. 파일 맵은 `shell.md` 안에 있다 — 따로 만들지 않는다.
