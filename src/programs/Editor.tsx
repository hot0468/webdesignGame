/** `에디터` 창(VS코드). 사이트 업무의 **퍼블리싱 공정**이 여기서 돌아간다 —
 *  피그마 시안 다음 차례다.
 *
 * ⚠️ 아직 공정 시스템이 없어 실행할 것이 없다 — 빈 상태로 무엇이 여기 뜰지만 적는다.
 *    (`Figma.tsx`와 같은 이유다.) */
export function Editor() {
  return (
    <div className="empty">
      <p className="empty__title">열어 둔 작업물이 없다</p>
      <p className="empty__note">
        시안이 끝난 사이트 업무를 여기서 퍼블리싱한다. 코딩 숙련도가 드는 행동력을 깎는다.
      </p>
    </div>
  )
}
