/** 납품 뒤에 클라이언트가 **다시 말을 거는 축**의 수치·문안. 판정은 전부
 *  `systems/followup.ts`의 순수 함수가 진다(여기는 값과 글자만 산다).
 *
 * 세 가지가 한 시스템이다:
 *   ① 수정 요청 — 회신했는데 "이렇게 말고 저렇게"라며 **그 공정이 도로 열린다**
 *   ② 클라이언트 성격 — 업체마다 성격이 있어 ①·③의 확률과 말투가 갈린다
 *   ③ 크로스브라우징 버그 리포트 — 납품 몇 주 뒤 "사파리에서 깨져요"가 유지보수로 돌아온다
 *
 * ⚠️ 성격은 **확률 배율과 문안만** 정한다. 마감·대금·정산에 손대지 않는다 —
 *    새 축을 만들면 정산이 무엇 때문에 흔들렸는지 읽을 수 없게 된다. */

/** 클라이언트 성격 하나. `mult`가 수정·버그 확률에 곱해지는 유일한 값이다. */
export type Personality = {
  id: 'easy' | 'ghost' | 'rush' | 'pixel'
  label: string
  /** 화면에 한 줄로 붙는 설명(`components/JobActions.tsx`). */
  desc: string
  /** 수정·버그 확률 배율. 1보다 작으면 순한 담당자다. */
  mult: number
  /** 수정 요청 메일의 본문 문안. ⚠️ **최소 3개**를 둔다 — 매번 같은 문장이면
   *  두 번째 수정 요청부터는 아무도 읽지 않는다. */
  revise: readonly string[]
}

/** 성격 4종. **업체 이름 하나에서 파생하고 저장하지 않는다**(`systems/followup.ts`의
 *  `personalityOf` — `clientKeywords`·`applicants`와 같은 규칙).
 *
 * ⚠️ 순서를 바꾸면 같은 업체의 성격이 바뀐다(`pick`이 자리로 고른다). 늘리거나 줄일 때는
 *    그 판이 통째로 달라진다는 것을 알고 하라. */
export const PERSONALITIES: readonly Personality[] = [
  {
    id: 'easy',
    label: '다맡김형',
    desc: '전문가한테 맡겼으니 알아서 해 달라는 쪽이다.',
    mult: 0.4,
    revise: [
      '전문가시니까 알아서 해 주시면 되는데, 딱 하나만요. 로고가 조금 작아 보이는 것 같기도 하고요. 편하신 대로 한 번만 더 봐 주세요.',
      '저는 잘 몰라서요. 주변에서 보더니 좀 밋밋하다고 하네요. 알아서 예쁘게 한 번만 더 부탁드립니다.',
      '다 좋습니다. 그런데 사장님이 한 번만 다시 보자고 하셔서요. 죄송하지만 한 번만 더요.',
    ],
  },
  {
    id: 'ghost',
    label: '잠수형',
    desc: '연락이 뜸하다가 한참 뒤에야 말을 꺼낸다.',
    mult: 0.7,
    revise: [
      '늦게 연락드려 죄송합니다. 그동안 내부에서 얘기가 좀 있었는데요, 방향을 처음 말씀드린 것과 다르게 가기로 했습니다. 다시 부탁드려요.',
      '한참 못 봤네요. 지금 열어 보니 이건 저희가 생각한 게 아닌 것 같습니다. 다시 한번 부탁드립니다.',
      '회신이 늦었습니다. 담당자가 바뀌어서요. 새 담당자 의견으로 다시 작업해 주실 수 있을까요.',
    ],
  },
  {
    id: 'rush',
    label: '급발진형',
    desc: '지금 당장을 원하고 말이 자주 바뀐다.',
    mult: 1.3,
    revise: [
      '급하게 죄송한데 방향을 바꿔야 할 것 같습니다. 지금 당장 다시 해 주실 수 있나요? 오늘 안에 보고를 들어가야 해서요.',
      '아 그리고 이건 빼고요. 어제 말씀드린 건 없던 걸로 해 주세요. 다시 처음 방향으로요. 빨리 부탁드립니다.',
      '일단 다 만들어 놓고 보죠. 그리고 나서 정하겠습니다. 되는 대로 빨리요.',
    ],
  },
  {
    id: 'pixel',
    label: '픽셀간섭형',
    desc: '2px 단위로 본다.',
    mult: 1.8,
    revise: [
      '전체적으로 2px만 더 왼쪽으로 옮겨 주세요. 그리고 로고는 키우되 크기는 그대로 가는 방향으로 부탁드립니다.',
      '빨간색인데 좀 더 파란 느낌으로 바꿔 주세요. 튀지 않으면서 눈에 확 들어오게요.',
      '3안이 제일 나은데 1안 색으로 바꿔서 2안 배치로 주시면 됩니다. 폰트는 지난번 것으로 다시요.',
      '자간이 살짝 넓은 것 같습니다. 그리고 여백은 시원하게, 대신 내용은 한 화면에 다 들어오게 해 주세요.',
    ],
  },
]

// ── 수정 요청 ────────────────────────────────────────────────
/** 한 업무에 올 수 있는 수정 요청의 상한. ⚠️ **상한이 없으면 판이 끝나지 않는다** —
 *  공정이 계속 도로 열려 대금이 영원히 안 들어오는 업무가 생긴다. */
export const REVISION_MAX = 2

/** 기본 수정 확률(CS 0 기준). ⚠️ 스펙에 없는 **임시치**다.
 *
 * ⚠️ 이 값은 **회신 한 번당**이다 — 사이트 업무는 공정이 셋이라 한 건에서 세 번 굴린다.
 *    0.5면 사이트 한 건에 수정이 한 번이라도 붙을 확률이 이미 88%다. 성격 배율(최대 1.8)이
 *    여기 곱해진다는 것까지 함께 보고 올려라. */
export const REVISION_BASE = 0.35

/** CS 스탯 1당 깎이는 수정 확률. **CS가 사는 두 번째 자리다**(첫째는 `csRecover`).
 *
 * ⚠️ 스펙에 없는 임시치다. CS 100이면 0.4 → 0.15로 눈에 띄게 줄지만 **0이 되지는 않는다** —
 *    0이면 CS만 올려 놓고 무엇을 만들든 한 번에 통과하는 판이 된다. */
export const CS_REVISION_WEIGHT = 0.0025

/** 성격 배율까지 곱한 뒤의 상한. ⚠️ 픽셀간섭형(1.8배)이 이 값을 넘겨 "거의 매번 수정"이
 *  되면 그 업체 일은 받지 않는 것이 늘 정답이 된다.
 *  ⚠️ **반반이 상한이다.** 성격을 아는 것은 수주한 뒤라 피할 수 없는 축이고, 피할 수 없는
 *     것이 절반을 넘으면 벌이 아니라 세금이 된다. */
export const REVISION_CHANCE_MAX = 0.5

/** 수정 요청이 오면 마감이 이만큼 밀린다. ⚠️ **0으로 두지 마라** — 다시 만드는 행동력만으로도
 *  이미 벌인데 마감까지 그대로면, 기한이 임박한 업무는 수정 요청 한 통에 그대로 계약 파기가
 *  된다(플레이어가 손쓸 수 있는 것이 아무것도 없는 즉사다). */
export const REVISION_DUE_EXTRA = 1

// ── 크로스브라우징 버그 리포트 ────────────────────────────────
/** 기본 버그 확률(코딩 숙련도 0 기준). ⚠️ 스펙에 없는 임시치다. */
export const BUG_BASE = 0.3

/** 코딩 숙련도 1당 깎이는 버그 확률. **코딩 숙련도가 사는 두 번째 자리다**(첫째는 `timeCost`) —
 *  익혀서 빨라지는 것에 **덜 깨진다**가 더해져야 숙련도를 올리는 이유가 하나 더 생긴다.
 *  ⚠️ 숙련도 100이면 0.02까지 내려간다(0은 아니다 — 완전히 사라지면 이 축 자체가 없어진다). */
export const BUG_SKILL_WEIGHT = 0.0028

/** 성격 배율까지 곱한 뒤의 상한. */
export const BUG_CHANCE_MAX = 0.5

/** 납품 몇 주 뒤에 신고가 오는가. **이 값이 도착 시점을 만든다** —
 *  `Message.week`에 실려 `inbox()`가 그 주가 될 때까지 목록에서 뺀다. */
export const BUG_DELAY_WEEKS = 2

/** 버그 수정 의뢰의 기한. */
export const BUG_DUE_WEEKS = 2

/** 신고 문안. 브라우저·기기·증상을 한 줄로 묶어 둔다 — 조각으로 나눠 조립하면
 *  "IE에서 폰트가 안 열림" 같은 말이 안 되는 신고가 나온다. */
export const BUG_SYMPTOMS: readonly string[] = [
  '사파리에서 상단 메뉴를 눌러도 안 열린다고 합니다. 크롬에서는 잘 된다는데요.',
  '아이폰으로 보면 메인 이미지가 옆으로 잘려 나옵니다. 스크롤도 가로로 움직여요.',
  '아직 인터넷 익스플로러 쓰시는 고객님이 계신데, 그쪽에서는 화면이 아예 하얗게 뜬다고 합니다.',
  '안드로이드 크롬에서 글씨가 다 깨져 보인다고 연락이 왔습니다. 네모로 나온다네요.',
  '태블릿 가로로 돌리면 하단 푸터가 본문 위로 겹쳐 올라옵니다.',
  '엣지에서 문의 폼을 보내면 아무 반응이 없다고 합니다. 버튼만 눌리고 끝이라네요.',
  '갤럭시 기본 브라우저에서 팝업이 화면 밖으로 나가서 닫기 버튼이 안 보입니다.',
]
