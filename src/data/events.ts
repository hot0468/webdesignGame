/** 특수 이벤트 셋의 수치. **좋은 일 둘과 나쁜 일 하나**다.
 *
 * ⚠️ 이 게임은 오래 **나쁜 일만 있었다**(클레임·파기·퇴사·버그). 잘해도 조용하고
 *    못하면 시끄러운 판이라, 평판을 올릴 이유가 대금 배율뿐이었다. 소개와 수상이
 *    그 빈자리를 메운다 — **잘한 것에도 사건이 붙는다.**
 *
 * ⚠️ 확률은 전부 **주당**이고 시드는 주차에서 파생한다(`systems/`가 `Math.random`을
 *    쓰지 않는다). 값은 스펙에 없는 임시치다. */

/** ── 소개·추천 ──────────────────────────────────────────
 *  평판이 높으면 거래하던 업체가 아는 곳을 소개해 준다. */

/** 소개가 오기 시작하는 평판. ⚠️ 시작 평판(30)보다 **높아야** 한다 — 처음부터 오면
 *  "평판을 올린 보상"이 아니라 그냥 시간이 주는 것이 된다. */
export const REFERRAL_MIN_REPUTATION = 50

/** 그 주에 소개가 올 확률. ⚠️ 너무 높으면 업체가 금방 동나 이벤트가 한 철에 끝난다. */
export const REFERRAL_CHANCE = 0.25

/** ── 수상 ───────────────────────────────────────────────
 *  어워더즈가 좋은 작업물을 뽑아 상을 준다. */

/** 후보가 되는 최소 등급. ⚠️ 포트폴리오 기준(`PORTFOLIO_MIN_GRADE`)과 같은 자리를
 *  본다 — "자랑할 만한 것"의 기준이 둘이면 플레이어가 어느 쪽을 믿을지 헷갈린다. */
export const AWARD_MIN_GRADE = 'A'

/** 그 주에 수상할 확률(후보가 있을 때). */
export const AWARD_CHANCE = 0.15

/** 수상이 올리는 평판. ⚠️ 클레임 하나(`CLAIM_REPUTATION_LOSS` 5)보다 커야 "잘한 것이
 *  더 크게 남는다"가 성립한다 — 작으면 상을 받아도 사고 한 번에 지워진다. */
export const AWARD_REPUTATION = 8

/** 상금. **돈보다 평판이 주인공이다** — 크게 주면 수상이 돈벌이 수단이 되어
 *  업무를 제대로 하는 것보다 상을 노리는 것이 최적이 된다. */
export const AWARD_PRIZE = 500_000

/** ── 저작권 위반 ────────────────────────────────────────
 *  웹폰트·이미지를 잘못 쓴 건으로 법무사무실에서 연락이 온다. */

/** 그 주에 연락이 올 확률. ⚠️ **낮게 둔다** — 플레이어가 막을 수 없는 벌이라
 *  자주 오면 억울함만 쌓인다(막을 수 있는 벌은 클레임·파기 쪽이 이미 진다). */
export const COPYRIGHT_CHANCE = 0.04

/** 합의금. ⚠️ **파기(대금 0)보다 작게** 둔다 — 내 잘못이 아닌 일이 가장 큰 벌이면
 *  게임이 불공평하게 느껴진다. */
export const COPYRIGHT_FEE = 800_000

/** 저작권 건이 깎는 평판. 돈이 주된 벌이라 평판은 가볍게 문다. */
export const COPYRIGHT_REPUTATION = 3

/** 연락이 오기 시작하는 주차. ⚠️ 첫 판부터 오면 아직 만든 것도 없는데 합의금부터
 *  무는 판이 된다 — 납품이 쌓일 시간을 준다. */
export const COPYRIGHT_FROM_WEEK = 8
